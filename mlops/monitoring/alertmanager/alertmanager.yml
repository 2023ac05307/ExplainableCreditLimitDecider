route:
  receiver: "default"
  group_wait: 15s
  group_interval: 2m
  repeat_interval: 2h

receivers:
- name: "default"
  # Add slack_configs / email_configs here
  # slack_configs:
  # - api_url: ${SLACK_WEBHOOK}
  #   channel: "#ml-alerts"
  #   title: "{{ .CommonAnnotations.summary }}"
  #   text: "{{ .CommonAnnotations.description }}"
